[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "imagecontexter"
version = "0.1.0"
description = "Classify images into category folders using a local VLM"
requires-python = ">=3.10"